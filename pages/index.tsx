import type { NextPage } from 'next';
import Layout from '~/components/Layout';

const Home: NextPage = () => {
	return (
		<Layout title={'AeroPress Tracker'}>
			<h1>Home Page</h1>
		</Layout>
	);
};

export default Home;
