import React from 'react';

interface Props {}

const Logo = ({}: Props) => {
	return (
		<div className="text-2xl md:text-3xl font-bold font-headline text-neutral-800">
			AeroPress Tracker
		</div>
	);
};

export default Logo;
