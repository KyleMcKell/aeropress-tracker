import React from 'react';

interface Props {}

const Logo = ({}: Props) => {
	return (
		<div className="text-3xl font-bold font-headline text-neutral-800">
			Aeropress Tracker
		</div>
	);
};

export default Logo;
